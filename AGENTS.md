# Agent Instructions

## XML documentation

- Use `<see cref="..."/>` when referencing types or members so generated docs retain links.
- Use explicit overload references, such as `<see cref="ForServiceUrl(Uri)"/>`, when a member has multiple overloads.
- Use `<c>...</c>` only for inline code formatting when a doc link is not intended.
